a.number? and variable? 都是原子操作，不需要定义多个
b.
(define (install-operator-package)
	(define (sum-deriv) ())
	(define (product-deriv) ())
	(put '+ '(deriv) sum-deriv)
	(put '* '(deriv) product-deriv)
	'done)
c.the same as b
d.nothing